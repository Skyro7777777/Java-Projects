name: macOS GUI (Simple "mac" password)
on: workflow_dispatch

jobs:
  gui:
    runs-on: macos-latest
    steps:
      - name: Install Tailscale
        run: |
          brew install tailscale
          sudo tailscaled --tun=userspace-networking &
          sleep 5
          sudo tailscale up --authkey ${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=github-macos-simple
          for i in {1..10}; do
            IP=$(sudo tailscale ip -4 2>/dev/null || true)
            if [ -n "$IP" ]; then
              echo "TAILSCALE_IP=$IP" >> $GITHUB_ENV
              break
            fi
            sleep 5
          done

      - name: Create user with simple password "mac"
        run: |
          sudo dscl . -create /Users/vncuser
          sudo dscl . -create /Users/vncuser UserShell /bin/bash
          sudo dscl . -create /Users/vncuser RealName "VNC User"
          sudo dscl . -create /Users/vncuser UniqueID "510"
          sudo dscl . -create /Users/vncuser PrimaryGroupID 20
          sudo dscl . -create /Users/vncuser NFSHomeDirectory /Users/vncuser
          sudo dscl . -passwd /Users/vncuser "mac"
          sudo dscl . -append /Groups/admin GroupMembership vncuser
          sudo createhomedir -c -u vncuser
          echo "✅ Created user: vncuser (password: mac)"

      - name: Enable Screen Sharing with VNC password "mac"
        run: |
          # Enable the VNC server
          sudo defaults write /var/db/launchd.db/com.apple.launchd/overrides.plist com.apple.screensharing -dict Disabled -bool false
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null || true

          # Set VNC password to "mac" (3 chars, well under 8 char limit)
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -configure -clientopts -setvncpw -vncpw "mac"

          # Restart the service
          sudo launchctl unload /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist
          
          sleep 5
          echo "✅ VNC server ready with password: mac"

      - name: Wake display
        run: |
          brew install cliclick
          cliclick c:.

      - name: Keep alive and show connection info
        run: |
          echo "========================================================="
          echo "✅ VNC SERVER READY"
          echo "========================================================="
          echo "Tailscale IP: ${{ env.TAILSCALE_IP }}"
          echo ""
          echo "📱 CONNECTION INSTRUCTIONS (ANDROID):"
          echo "----------------------------------------"
          echo "1. Open your VNC client (bVNC, RealVNC, etc.)"
          echo "2. Create new connection with:"
          echo "   - Connection Type: Basic VNC"
          echo "   - VNC Server: ${{ env.TAILSCALE_IP }}:5900"
          echo "   - VNC Username: (leave BLANK)"
          echo "   - VNC Password: mac"
          echo ""
          echo "3. After connecting, you'll see macOS login screen"
          echo "4. Log in with:"
          echo "   - Username: vncuser"
          echo "   - Password: mac"
          echo ""
          echo "⏳ Job will stay alive for 6 hours"
          echo "========================================================="
          while true; do sleep 3600; done