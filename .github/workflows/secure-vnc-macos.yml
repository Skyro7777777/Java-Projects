name: macOS GUI (RustDesk - Simple)
on: workflow_dispatch

jobs:
  gui:
    runs-on: macos-latest
    steps:
      - name: Install Tailscale
        run: |
          brew install tailscale
          sudo tailscaled --tun=userspace-networking &
          sleep 5
          sudo tailscale up --authkey ${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=github-macos-rustdesk || true
          IP=$(sudo tailscale ip -4 2>/dev/null || echo "unknown")
          echo "TAILSCALE_IP=$IP" >> $GITHUB_ENV

      - name: Create user vncuser
        run: |
          sudo dscl . -create /Users/vncuser
          sudo dscl . -create /Users/vncuser UserShell /bin/bash
          sudo dscl . -create /Users/vncuser RealName "VNC User"
          sudo dscl . -create /Users/vncuser UniqueID "510"
          sudo dscl . -create /Users/vncuser PrimaryGroupID 20
          sudo dscl . -create /Users/vncuser NFSHomeDirectory /Users/vncuser
          sudo dscl . -passwd /Users/vncuser "Apple@123"
          sudo dscl . -append /Groups/admin GroupMembership vncuser
          sudo createhomedir -c -u vncuser

      - name: Download & Install RustDesk
        run: |
          curl -L -o /tmp/rustdesk.dmg "https://github.com/rustdesk/rustdesk/releases/download/1.4.6/rustdesk-1.4.6-x86_64.dmg"
          hdiutil attach /tmp/rustdesk.dmg -nobrowse -mountpoint /Volumes/RustDesk
          cp -R "/Volumes/RustDesk/RustDesk.app" /Applications/
          hdiutil detach "/Volumes/RustDesk"

      - name: Reset Screen Recording permissions (this is the key step!)
        run: |
          # Reset the entire Screen Recording permission database
          # This clears any stale permissions and ensures a fresh prompt
          sudo tccutil reset ScreenCapture
          echo "✅ Screen Recording permissions reset. RustDesk will now prompt."

      - name: Launch RustDesk and capture ID
        run: |
          sudo -u vncuser open /Applications/RustDesk.app
          sleep 15  # Wait for it to launch and generate ID
          
          # Take screenshot (now that permissions are reset, it should work)
          sudo -u vncuser screencapture -x /tmp/rustdesk.png
          
          # Extract ID with OCR
          brew install tesseract
          tesseract /tmp/rustdesk.png stdout 2>/dev/null | grep -Eo '[0-9]{9}' | head -1 > /tmp/rustdesk_id.txt
          echo "RUSTDESK_ID=$(cat /tmp/rustdesk_id.txt)" >> $GITHUB_ENV

      - name: Upload screenshot
        uses: actions/upload-artifact@v4
        with:
          name: rustdesk-credentials
          path: /tmp/rustdesk.png

      - name: Keep alive
        run: |
          echo "=================================================="
          echo "RustDesk ID: ${{ env.RUSTDESK_ID }}"
          echo "Password: (check the RustDesk window)"
          echo "Tailscale IP: ${{ env.TAILSCALE_IP }}"
          echo "=================================================="
          while true; do sleep 3600; done