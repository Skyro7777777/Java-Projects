name: macOS GUI (AskLink via App Store)
on: workflow_dispatch

jobs:
  gui:
    runs-on: macos-latest
    steps:
      - name: Install Tailscale
        run: |
          brew install tailscale
          sudo tailscaled --tun=userspace-networking &
          sleep 5
          sudo tailscale up --authkey ${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=github-macos-asklink
          for i in {1..10}; do
            IP=$(sudo tailscale ip -4 2>/dev/null || true)
            if [ -n "$IP" ]; then
              echo "TAILSCALE_IP=$IP" >> $GITHUB_ENV
              break
            fi
            sleep 5
          done

      - name: Create user vncuser with password Apple@123
        run: |
          sudo dscl . -create /Users/vncuser
          sudo dscl . -create /Users/vncuser UserShell /bin/bash
          sudo dscl . -create /Users/vncuser RealName "VNC User"
          sudo dscl . -create /Users/vncuser UniqueID "510"
          sudo dscl . -create /Users/vncuser PrimaryGroupID 20
          sudo dscl . -create /Users/vncuser NFSHomeDirectory /Users/vncuser
          sudo dscl . -passwd /Users/vncuser "Apple@123"
          sudo dscl . -append /Groups/admin GroupMembership vncuser
          sudo createhomedir -c -u vncuser
          echo "✅ Created user: vncuser (password: Apple@123)"

      - name: Enable Screen Sharing (VNC) with password Apple@12
        run: |
          sudo defaults write /var/db/launchd.db/com.apple.launchd/overrides.plist com.apple.screensharing -dict Disabled -bool false
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null || true
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -configure -clientopts -setvncpw -vncpw "Apple@12"
          sudo launchctl unload /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist
          sleep 5
          echo "✅ VNC ready on port 5900 (password: Apple@12)"

      - name: Open App Store for AskLink installation (manual step)
        run: |
          echo "========================================================="
          echo "📱 INSTRUCTIONS FOR NEXT STEP"
          echo "========================================================="
          echo "1. Connect via VNC to ${{ env.TAILSCALE_IP }}:5900"
          echo "   VNC Password: Apple@12"
          echo ""
          echo "2. Even if the screen is black, the desktop is live."
          echo "   Follow the navigation guide to install AskLink:"
          echo ""
          echo "   a) Press Cmd+Space → type 'App Store' → Enter"
          echo "   b) Click search (top-right) → type 'AskLink' → Enter"
          echo "   c) Click 'Get' (center of screen) → Click 'Install'"
          echo "   d) Press Cmd+Space → type 'AskLink' → Enter"
          echo "   e) Note the Device ID and password shown"
          echo ""
          echo "3. On your Android, install AskLink from Play Store"
          echo "4. Enter the Device ID and password"
          echo "5. You should see the macOS desktop!"
          echo "========================================================="

      - name: Wake display
        run: |
          brew install cliclick
          cliclick c:. m:0,0 c:.

      - name: Keep alive
        run: |
          echo "⏳ Job will stay alive for 6 hours."
          while true; do sleep 3600; done