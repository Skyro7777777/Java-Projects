name: macOS GUI (VNC + Tailscale)
on: workflow_dispatch

jobs:
  gui:
    runs-on: macos-latest
    steps:
      - name: Install Tailscale
        run: |
          brew install tailscale
          sudo tailscaled --tun=userspace-networking &
          sleep 5
          sudo tailscale up --authkey ${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=github-macos-final
          for i in {1..10}; do
            IP=$(sudo tailscale ip -4 2>/dev/null || true)
            if [ -n "$IP" ]; then
              echo "TAILSCALE_IP=$IP" >> $GITHUB_ENV
              break
            fi
            sleep 5
          done

      - name: Enable Screen Sharing (VNC) with correct password
        run: |
          # Enable the VNC server
          sudo defaults write /var/db/launchd.db/com.apple.launchd/overrides.plist com.apple.screensharing -dict Disabled -bool false
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null || true

          # Set the VNC password (max 8 chars)
          VNC_PASS="Apple@12"
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -configure -clientopts -setvncpw -vncpw "$VNC_PASS"

          # Restart the service to apply
          sudo launchctl unload /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist

          # Verify
          sleep 5
          if sudo lsof -i :5900 > /dev/null; then
            echo "✅ VNC server is listening on port 5900 with password $VNC_PASS"
          else
            echo "❌ VNC server failed to start"
            exit 1
          fi

      - name: Wake up the display
        run: |
          # Install cliclick to simulate mouse movement
          brew install cliclick
          # Move mouse slightly to wake display
          cliclick c:.
          echo "✅ Display woken"

      - name: Keep alive and show connection info
        run: |
          echo "========================================================="
          echo "✅ VNC server ready"
          echo "========================================================="
          echo "Tailscale IP: ${{ env.TAILSCALE_IP }}"
          echo ""
          echo "🔹 Connect with any VNC client to ${{ env.TAILSCALE_IP }}:5900"
          echo "   VNC password: Apple@12"
          echo ""
          echo "👉 You should see the desktop of the 'runner' user."
          echo ""
          echo "⏳ Job will stay alive for 6 hours."
          echo "========================================================="
          while true; do sleep 3600; done
